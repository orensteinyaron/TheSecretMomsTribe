node_modules/
.env
data/*.json
.claude/settings.local.json
app/dist/

# Avatar Full v5 face-metrics sidecar — venv + lazily-downloaded model.
bin/face-metrics/.venv/
bin/face-metrics/models/

# Playwright MCP scratch + local carousel build artifacts (assets live in Supabase storage)
.playwright-mcp/

# avatar render working dir (transient artifacts)
video/workdir/
